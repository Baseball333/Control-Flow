found = 0;
for (i = 0; i < n && !found; i++)
  for (j = 0; j < n && !found j++)
    if (a[i] == b[j])
         found = 1;
    if (found)
      /* Got one: a[i-1] == b[j] */
    else
      /* Didn't find any common elements */
